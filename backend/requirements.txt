fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.4
pydantic-settings>=2.2.1
python-multipart>=0.0.9
sqlalchemy[asyncio]>=2.0.28
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
alembic>=1.13.1
redis>=5.0.3
fakeredis>=2.21.0
openpyxl>=3.1.2
pandas>=2.2.1
python-docx>=1.1.0
pillow>=10.2.0
pdfplumber>=0.11.0
pypdf>=4.1.0
email-validator>=2.0.0
bcrypt>=4.1.2
httpx>=0.27.0
pytest>=8.1.1
pytest-asyncio>=0.23.5
pytest-cov>=4.1.0
matplotlib>=3.8.0
# Local tally reader, used when the online reader is busy or offline.
# rapidocr runs PaddleOCR's own models without the PaddlePaddle framework.
# (The older rapidocr-onnxruntime package has no Python 3.13 build, which is
# why no local reader was actually installed during the 23 Sep demo.)
rapidocr>=3.0
onnxruntime>=1.20
pytesseract>=0.3.10
# Optional and heavy (torch is ~2 GB). Uncomment only if needed.
# easyocr>=1.7.2
# torch>=2.0.0
# torchvision>=0.15.0
